# CF-visualization

貸借対照表(BS)・損益計算書(PL)・株主資本等変動計算書(SS)をまとめた**1つのCSVファイルをアップロード**すると、間接法によるキャッシュ・フロー計算書(CF計算書)を自動作成してWebページ上に表示する静的Webアプリです。

## 機能

- **CSVアップロード** — ドラッグ&ドロップまたはファイル選択。BS・PL・SSを1つのファイルにまとめて読み込みます
- **文字コード自動判定** — UTF-8(BOM有無どちらも)と Shift_JIS(Excel保存のCSV)に対応
- **表記ゆれの吸収** — 科目名の別名、`△1,234` / `(1,234)` / `¥1,234` / 全角数字などの金額表記に対応
- **CF計算書の自動生成(間接法)** — 営業・投資・財務の3区分と小計・期首/期末残高を自動計算
- **ウォーターフォールチャート** — 期首残高 → 営業CF → 投資CF → 財務CF → 期末残高の推移を可視化(ホバーで詳細表示)
- **整合性チェック** — BSの貸借一致、CF計算書とBSの現金残高の一致、SSと純資産増減の整合を自動検証
- **テンプレートCSVのダウンロード** — 空欄のひな形を出力。読み込んだ内容をCSVに書き戻すこともできます
- **ダークモード対応**・**入力の自動保存**(次回アクセス時に復元)

## 使い方

1. `index.html` をブラウザで開く(ビルド不要・依存ライブラリなし)
2. 「テンプレートCSVをダウンロード」でひな形を取得し、金額を入力する
3. できあがったCSVをドラッグ&ドロップする → CF計算書とチャートが表示される

まず「サンプルCSVを読み込む」ボタンで動作を確認するのがおすすめです(同じ内容が `sample.csv` にあります)。

ローカルでサーバーを立てる場合:

```bash
python3 -m http.server 8000
# http://localhost:8000 を開く
```

### GitHub Pagesで公開する場合

リポジトリの Settings → Pages で、ブランチを選んで公開するだけで動作します(静的サイトのため追加設定は不要)。

## CSVの書式

1行目に見出し、2行目以降に科目と金額を並べた**縦持ち形式**です。

```csv
区分,科目,前期末,当期末
BS,現金及び預金,12000,7900
BS,売上債権,18000,21000
PL,税引前当期純利益,,9000
補足,有形固定資産の売却による収入,,1800
SS,剰余金の配当,,1800
```

- **`区分` 列は任意**です(読み飛ばします)。科目名から自動で振り分けます。
- **BSは前期末・当期末の2列**、**PL・SS・補足情報は当期末の列だけ**を使います。
- 見出しの名前には幅があります — 科目列は `科目` / `項目` / `勘定科目` / `変動事由`、金額列は `前期末` / `前期` / `期首`、`当期末` / `当期` / `期末` / `金額` などが使えます。
- 見出し行より上にタイトル行や単位の行があっても読み飛ばします。見出し行がない場合は列位置を推定します。
- 区切り文字はカンマとタブ(TSV)の自動判定です。`#` で始まる行はコメントとして無視します。
- 金額は `1,234` / `¥1,234` / `△1,234` / `▲1,234` / `(1,234)` / `-1,234` / 全角数字のいずれでも読み取れます。
- **同じ科目に複数行あれば合算します**(例:`受取手形` と `売掛金` の2行 → `売上債権`)。
- 認識できなかった科目の行(小計行など)は読み飛ばし、画面に一覧で表示します。
- SS・補足情報の金額は、**支出・減少項目もすべて正の値**で入力してください(負の値は絶対値として扱い、警告を表示します)。

### 科目名と別名

| 区分 | 科目(正式名) | 使える別名の例 |
|---|---|---|
| BS | 現金及び預金 | 現金、現金預金、現預金、現金及び現金同等物 |
| BS | 売上債権 | 売掛金、受取手形、受取手形及び売掛金 |
| BS | 棚卸資産 | 商品、製品、仕掛品、原材料、在庫 |
| BS | その他流動資産 | その他の流動資産、前払費用、未収入金 |
| BS | 有形固定資産(純額) | 有形固定資産、建物、機械装置、土地 |
| BS | 投資その他の資産 | 投資等、投資有価証券 |
| BS | 仕入債務 | 買掛金、支払手形、支払手形及び買掛金 |
| BS | 短期借入金 | 短期借入、1年内返済予定の長期借入金 |
| BS | その他流動負債 | その他の流動負債、未払金、未払費用、未払法人税等 |
| BS | 長期借入金 | 長期借入、社債 |
| BS | 純資産合計 | 純資産、株主資本、株主資本合計 |
| PL | 税引前当期純利益 | 税金等調整前当期純利益、税引前利益 |
| PL | 減価償却費 | 償却費 |
| PL | 受取利息及び受取配当金 | 受取利息、受取配当金 |
| PL | 支払利息 | 支払利息割引料 |
| PL | 固定資産売却損益 | 固定資産売却益、固定資産売却損(符号を反転して取り込みます) |
| PL | 法人税等 | 法人税、住民税及び事業税 / 法人税等合計 |
| 補足 | 有形固定資産の売却による収入 | 固定資産売却収入 |
| SS | 新株の発行(増資) | 増資、株式の発行 |
| SS | 剰余金の配当 | 配当金の支払額、配当金 |
| SS | 自己株式の取得 | 自己株式取得 |
| SS | 自己株式の処分 | 自己株式処分 |

別名は `js/schema.js` の `aliases` で定義しているので、自社の勘定科目名に合わせて追加できます。

## 計算ロジック(間接法)

### 営業活動によるキャッシュ・フロー

税引前当期純利益に以下を加減算します。

- 減価償却費(+)
- 受取利息及び受取配当金(−)/ 支払利息(+)
- 固定資産売却損益(益は−、損は+)
- 売上債権・棚卸資産・その他流動資産の増減(増加は−)
- 仕入債務・その他流動負債の増減(増加は+)
- 小計の後、利息及び配当金の受取額(+)、利息の支払額(−)、法人税等の支払額(−)

### 投資活動によるキャッシュ・フロー

- 有形固定資産の取得による支出 — `期末純額 − 期首純額 + 減価償却費 + 売却簿価` で逆算(売却簿価 = 売却収入 − 売却損益)
- 有形固定資産の売却による収入(補足情報から)
- 投資その他の資産の増減(BS差額の純額)

### 財務活動によるキャッシュ・フロー

- 短期借入金・長期借入金の増減(BS差額の純額)
- 株式の発行による収入、自己株式の取得・処分、配当金の支払額(SSから)

### 簡便法としての前提

- 利息・配当金・法人税等の受払額は経過勘定を考慮せず、PLの発生額をそのまま使用
- 現金及び現金同等物 = BSの「現金及び預金」

## ファイル構成

```
index.html      # 画面(アップロード・読み込み結果・出力)
sample.csv      # サンプルデータ
css/style.css   # スタイル(ライト/ダークテーマ)
js/schema.js    # 科目定義と別名
js/csv.js       # CSVの文字コード判定・解析・科目の割り当て
js/app.js       # CF計算ロジック・CF計算書とチャートの描画
```
